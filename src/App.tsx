import React from "react";
import TopBar from "./components/TopBar";
import SideBar from "./components/SideBar";
const app = () => {
  return (
    <div>
      <TopBar></TopBar>
      <SideBar></SideBar>
    </div>
  );
};

export default app;
